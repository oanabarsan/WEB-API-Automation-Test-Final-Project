{
  "name": "api_web-automation-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "npx cypress open",
    "cy:chrome:headed": "npx cypress run --browser chrome --headed",
    "cy:chrome:headless": "npx cypress run --browser chrome",
    "cy:firefox:headed": "npx cypress run --browser firefox --headed",
    "api-test": "npx mocha api-tests --reporter mochawesome --reporter-options reportDir=Results,reportFilename=api-test-results --timeout 10000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "cypress": "^12.13.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "pactum": "^3.4.1"
  }
}
